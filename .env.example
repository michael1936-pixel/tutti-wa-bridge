# Generate a long random string (e.g. `openssl rand -hex 32`)
API_KEY=replace-me-with-a-long-random-string

# Optional — Railway sets PORT automatically. Leave blank in production.
# PORT=3001

# Where Baileys stores its session. Must match the Railway Volume mount path.
AUTH_DIR=/app/auth
